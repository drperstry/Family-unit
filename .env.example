# ===========================================
# FamilyHub Environment Configuration
# ===========================================
# Copy this file to .env.local and fill in your values
# NEVER commit .env.local to version control

# ===========================================
# DATABASE (Required)
# ===========================================
# MongoDB Connection String
#
# For local development:
#   MONGODB_URI=mongodb://localhost:27017/familyhub
#
# For MongoDB Atlas (recommended for Vercel):
#   1. Create a free cluster at https://www.mongodb.com/atlas
#   2. Create a database user with read/write access
#   3. Whitelist your IP (or use 0.0.0.0/0 for Vercel)
#   4. Get your connection string from Atlas Dashboard
#
# Format: mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<database>?retryWrites=true&w=majority
#
# IMPORTANT for Vercel:
#   - Use MongoDB Atlas (MongoDB's cloud service)
#   - Whitelist 0.0.0.0/0 in Network Access (Vercel uses dynamic IPs)
#   - Use connection pooling settings in the app (already configured)
#
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/familyhub?retryWrites=true&w=majority

# ===========================================
# AUTHENTICATION (Required)
# ===========================================
# JWT Secret Key - used to sign authentication tokens
# Generate a secure random string (minimum 32 characters)
# You can generate one with: openssl rand -base64 32
#
JWT_SECRET=your-super-secret-jwt-key-minimum-32-characters-change-in-production

# JWT Token Expiration (optional)
# Default: 7d (7 days)
JWT_EXPIRES_IN=7d

# ===========================================
# APPLICATION (Required)
# ===========================================
# Public URL of your application
# Local: http://localhost:3000
# Production: https://your-domain.vercel.app
#
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Environment: development, production, test
NODE_ENV=development

# ===========================================
# VERCEL BLOB STORAGE (Optional)
# ===========================================
# For file uploads (photos, documents)
# Get your token from Vercel Dashboard > Storage > Blob
# https://vercel.com/docs/storage/vercel-blob
#
BLOB_READ_WRITE_TOKEN=

# ===========================================
# GOOGLE MAPS (Optional)
# ===========================================
# For the family locations feature
# Get your API key from Google Cloud Console
# Enable: Maps JavaScript API, Places API
# https://console.cloud.google.com/google/maps-apis
#
GOOGLE_MAPS_API_KEY=

# ===========================================
# EMAIL / SMTP (Optional)
# ===========================================
# For sending notifications and password resets
# Works with any SMTP provider (SendGrid, Mailgun, etc.)
#
SMTP_HOST=smtp.sendgrid.net
SMTP_PORT=587
SMTP_USER=apikey
SMTP_PASSWORD=your-sendgrid-api-key
EMAIL_FROM=noreply@familyhub.com

# ===========================================
# RATE LIMITING (Optional)
# ===========================================
# API rate limiting configuration
# Default: 100 requests per minute
#
RATE_LIMIT_MAX=100
RATE_LIMIT_WINDOW_MS=60000

# ===========================================
# LOGGING (Optional)
# ===========================================
# Log level: debug, info, warn, error
LOG_LEVEL=info

# ===========================================
# VERCEL DEPLOYMENT NOTES
# ===========================================
# When deploying to Vercel:
#
# 1. Add all required environment variables in Vercel Dashboard
#    Settings > Environment Variables
#
# 2. For MONGODB_URI:
#    - Use MongoDB Atlas
#    - In Atlas Network Access, add 0.0.0.0/0 (allows all IPs)
#    - This is required because Vercel serverless functions use dynamic IPs
#
# 3. For sensitive variables (JWT_SECRET, MONGODB_URI):
#    - Enable "Sensitive" option in Vercel to mask the value
#
# 4. Environment scope:
#    - Production: Your main domain
#    - Preview: PR preview deployments
#    - Development: Local development (not used in Vercel)
#
# 5. After adding variables, redeploy your application
